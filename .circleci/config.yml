version: 2

references:
  restore_repo: &restore_repo
    restore_cache:
      keys:
        - v1-repo-{{ .Branch }}-{{ .Revision }}
        - v1-repo-{{ .Branch }}
        - v1-repo

jobs:
  build:
    docker:
      - image: eutambem/mobile-build:latest
    working_directory: ~/repo
    steps:
      - checkout
      - run: bundle install
      - run: yarn install
      - run: yarn test
      - run: bundle exec fastlane ios prepare_build
      - save_cache:
          key: v1-repo-{{ .Branch }}-{{ .Revision }}
          paths:
            - .

  deploy_ios:
    macos:
      xcode: "9.3.0"
    steps:
    working_directory: /Users/distiller/project
    environment:
    shell: /bin/bash --login -o pipefail
    steps:
      - *restore_repo
      - run: bundle exec fastlane ios beta


workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy_ios:
          requires:
            - build
